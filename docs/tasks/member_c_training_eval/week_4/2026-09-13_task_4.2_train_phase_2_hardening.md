# TASK 4.2: THỰC THI HUẤN LUYỆN PHA 2 (HARD DEGRADATION HARDENING) TRÊN A100

- **Mã nhiệm vụ**: `Task 4.2`
- **Người phụ trách**: **Thành viên C** *(Chủ trì)* phối hợp với **Thành viên B**
- **Thời hạn hoàn thành**: Tuần 4 (Ngày thứ 28 của Sprint)
- **Độ ưu tiên**: 🔴 Cốt lõi (Quyết định khả năng đột phá trên ảnh nén nặng)
- **Phụ thuộc (Dependencies)**: Đã hoàn tất Pha 1 và có checkpoint `fatformer_srm_phase1.pth`.

---

## 1. MỤC TIÊU KỸ THUẬT (OBJECTIVE)
1. Thực thi **Pha 2 của Chiến dịch Finetune** (Hard Degradation Hardening) trên Colab Pro A100:
   - Thời lượng: **4 epochs** (tiêu tốn ~35 Compute Units).
   - Nạp tiếp tục từ checkpoint Pha 1: `fatformer_srm_phase1.pth`.
2. Nâng cấp tỷ lệ suy biến dữ liệu lên mức gắt gao:
   - Tăng xác suất nén JPEG sâu ($Q \in [30, 60]$ chiếm $50\%$ tổng số ảnh).
   - Bổ sung Double-JPEG và Gaussian Blur mạnh ($\sigma \in [1.0, 3.0]$).
3. Giảm Learning Rate xuống mức tinh chỉnh mịn ($5 \times 10^{-5}$) để rèn luyện cổng Dynamic Gating $\lambda(x)$ học cách triệt tiêu dải tần số DWT khi gặp nén nặng.
4. Đạt được sản phẩm cốt lõi cuối cùng: **`fatformer_srm_robust_final.pth`**.

---

## 2. ĐẦU VÀO & TÀI NGUYÊN (INPUTS & RESOURCES)
* Checkpoint Pha 1: `fatformer_srm_phase1.pth`.
* Module transforms nâng cấp (hỗ trợ $Q=30..60$).
* ~35 Compute Units Colab Pro.

---

## 3. HƯỚNG DẪN THỰC HIỆN CHI TIẾT (STEP-BY-STEP)

### Cấu hình Tinh chỉnh Pha 2:
```python
# Thiết lập tham số Pha 2
EPOCHS_PHASE2 = 4
LR_ADAPTER_P2 = 5e-5    # Giảm 4 lần so với Pha 1 để bảo toàn tri thức
LR_GATING_P2 = 2e-4

# Nạp checkpoint Pha 1
print("[*] Nạp checkpoint fatformer_srm_phase1.pth để bắt đầu Pha 2...")
ckpt_p1 = torch.load("/content/drive/MyDrive/FatFormer_Hub/checkpoints/fatformer_srm_phase1.pth", map_location=device)
model.load_state_dict(ckpt_p1["state_dict"] if "state_dict" in ckpt_p1 else ckpt_p1)

# Cập nhật pipeline tăng cường ảnh sang nén sâu
from datasets.transforms import RandomJPEGCompression, DualStreamRobustAugmentation
train_dataset.transform = DualStreamRobustAugmentation(clean_prob=0.2) # 20% Clean, 80% Degraded
# Thiết lập nén sâu Q=30..60
train_dataset.transform.jpeg = RandomJPEGCompression(q_min=30, q_max=60)

# Cấu hình Optimizer mới
param_groups_p2 = [
    {"params": [p for n, p in model.named_parameters() if "freq_gating" in n and p.requires_grad], "lr": LR_GATING_P2},
    {"params": [p for n, p in model.named_parameters() if "freq_gating" not in n and p.requires_grad], "lr": LR_ADAPTER_P2}
]
optimizer_p2 = torch.optim.AdamW(param_groups_p2, weight_decay=1e-4)
scheduler_p2 = torch.optim.lr_scheduler.CosineAnnealingLR(optimizer_p2, T_max=EPOCHS_PHASE2*len(train_loader)//2)

print("[*] Bắt đầu Huấn luyện Pha 2 (Hard Degradation Hardening)...")
# Thực hiện vòng lặp huấn luyện tương tự Task 4.1 trong 4 epochs
```

---

## 4. SẢN PHẨM BÀN GIAO (DELIVERABLES)
1. Checkpoint hoàn chỉnh cốt lõi của đồ án: **`fatformer_srm_robust_final.pth`** lưu tại `FatFormer_Hub/checkpoints/` trên Drive 5TB.
2. File nhật ký huấn luyện `logs/phase2_train.log`.

---

## 5. TIÊU CHUẨN NGHIỆM THU (DEFINITION OF DONE - DoD)
- [ ] Hoàn thành 4 epochs Pha 2, không bị ngắt kết nối giữa chừng (hoặc nếu ngắt thì resume thành công).
- [ ] Chạy thử nghiệm trên tập test $Q=30$: Độ chính xác ACC đạt $\ge 85\%$ (tăng hơn $25\%$ so với baseline gốc).
- [ ] Độ chính xác trên tập Clean vẫn giữ vững $\ge 97.5\%$.
- [ ] Sẵn sàng chuyển giao checkpoint cuối cùng cho Tuần 5 Benchmark toàn diện.
