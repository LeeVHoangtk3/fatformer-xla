# TASK 1.2: XÂY DỰNG MÃ NGUỒN FATFORMER & TRÌNH NẠP KHỚP 1,116 TENSORS

- **Mã nhiệm vụ**: `Task 1.2`
- **Người phụ trách**: **Thành viên B** *(Architecture & Loss Lead)*
- **Thời hạn hoàn thành**: Tuần 1 (Ngày thứ 5 của Sprint)
- **Độ ưu tiên**: 🔴 Cực kỳ khẩn cấp (Nền móng cho toàn bộ pipeline mô hình)
- **Phụ thuộc (Dependencies)**: Đã có sẵn 2 tệp trọng số `ViT-L-14.pt` và `fatformer_4class_ckpt.pth`.

---

## 1. MỤC TIÊU KỸ THUẬT (OBJECTIVE)
1. Xây dựng cấu trúc thư mục package `models/` hoàn chỉnh cho mô hình FatFormer:
   - `models/fatformer.py`: Class `FatFormer` tổng hợp.
   - `models/faa.py`: Khối `ForgeryAwareAdapter` (Haar 2D DWT, Inter/Intra-band attention, `freq_scale`).
   - `models/lga.py`: Khối `LanguageGuidedAlignment` (chứa thuộc tính `self.patch_basaed_enhancer`).
2. Viết script kiểm thử `tests/verify_model_load.py` kiểm tra:
   - Nạp tệp [`ViT-L-14.pt`](file:///d:/GIT%20REPO/.nam4/fatformer-xla/ViT-L-14.pt) (TorchScript JIT) thành công.
   - Nạp tệp [`fatformer_4class_ckpt.pth`](file:///d:/GIT%20REPO/.nam4/fatformer-xla/fatformer_4class_ckpt.pth) với cờ `strict=True` khớp **1,116/1,116 tensors** và **933.16M tham số**, 0 missing keys, 0 unexpected keys.

---

## 2. ĐẦU VÀO & TÀI NGUYÊN (INPUTS & RESOURCES)
* Báo cáo giải phẫu trọng số: [`docs/paper_analysis/checkpoint_inspection_report.md`](file:///d:/GIT%20REPO/.nam4/fatformer-xla/docs/paper_analysis/checkpoint_inspection_report.md).
* Script thẩm định gốc: [`tools/check_weights.py`](file:///d:/GIT%20REPO/.nam4/fatformer-xla/tools/check_weights.py).
* Hai tệp trọng số nền tảng tại thư mục gốc repo.

---

## 3. HƯỚNG DẪN THỰC HIỆN CHI TIẾT (STEP-BY-STEP)

### Bước 1: Hiện thực hóa 2D Haar DWT thuần PyTorch trong `models/faa.py`
Tác giả gốc cài đặt biến đổi sóng con DWT và IDWT bằng ma trận tích chập cố định:
```python
import torch
import torch.nn as nn

class HaarDWT2D(nn.Module):
    """Biến đổi sóng con Haar 2D sử dụng Conv2d cố định."""
    def __init__(self):
        super().__init__()
        # Bộ lọc phân tách hàng và cột
        h0 = torch.tensor([1.0, 1.0]) / 2.0**0.5
        h1 = torch.tensor([-1.0, 1.0]) / 2.0**0.5
        
        self.register_buffer("h0_col", h0.view(1, 1, 2, 1))
        self.register_buffer("h1_col", h1.view(1, 1, 2, 1))
        self.register_buffer("h0_row", h0.view(1, 1, 1, 2))
        self.register_buffer("h1_row", h1.view(1, 1, 1, 2))

    def forward(self, x):
        # x: [B, C, H, W] -> Tách 4 dải tần LL, LH, HL, HH
        # Áp dụng tích chập với padding phản xạ đối xứng
        # Chi tiết xem trong checkpoint_inspection_report.md
        pass
```

### Bước 2: Khối LGA bắt buộc giữ lỗi chính tả trong `models/lga.py`
```python
class LanguageGuidedAlignment(nn.Module):
    def __init__(self, embed_dim=768):
        super().__init__()
        # BẮT BUỘC có chữ 'a' thừa trong 'basaed' để khớp state_dict của tác giả:
        self.patch_basaed_enhancer = nn.MultiheadAttention(embed_dim, num_heads=12)
        self.text_guided_interactor = nn.MultiheadAttention(embed_dim, num_heads=12)
```

### Bước 3: Viết script `tests/verify_model_load.py`
```python
import torch
from models.fatformer import build_fatformer

def test_load():
    print("[*] Đang khởi tạo kiến trúc mô hình FatFormer...")
    model = build_fatformer(pretrained_clip="ViT-L-14.pt")
    
    print("[*] Đang nạp checkpoint fatformer_4class_ckpt.pth...")
    ckpt = torch.load("fatformer_4class_ckpt.pth", map_location="cpu")
    state_dict = ckpt["model"] if "model" in ckpt else ckpt
    
    # Nạp với strict=True để kiểm tra tính toàn vẹn 100%
    incompatible = model.load_state_dict(state_dict, strict=True)
    print(f"[✓] Nạp THÀNH CÔNG pass strict=True!")
    print(f"[✓] Tổng tham số mô hình: {sum(p.numel() for p in model.parameters()):,}")

if __name__ == "__main__":
    test_load()
```

---

## 4. SẢN PHẨM BÀN GIAO (DELIVERABLES)
1. Thư mục mã nguồn [`models/`](file:///d:/GIT%20REPO/.nam4/fatformer-xla/models/) gồm đầy đủ các module con.
2. Script kiểm thử [`tests/verify_model_load.py`](file:///d:/GIT%20REPO/.nam4/fatformer-xla/tests/verify_model_load.py) chạy pass không có cảnh báo missing/unexpected key.

---

## 5. TIÊU CHUẨN NGHIỆM THU (DEFINITION OF DONE - DoD)
- [ ] Lệnh `python tests/verify_model_load.py` in ra `[✓] Nạp THÀNH CÔNG pass strict=True!`.
- [ ] Bàn giao class `FatFormer` cho Thành viên C tích hợp vào `train.ipynb`.
